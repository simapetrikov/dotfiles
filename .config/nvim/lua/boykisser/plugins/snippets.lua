return {
   "akinsho/toggleterm.nvim", event = "VeryLazy", version = "*",
   opts = {
      size = 10,
      open_mapping = [[<C-\>]], -- Shortcut for toggling the terminal
      start_in_insert = true, -- Start terminal in insert mode
      persist_size = true, -- Remember terminal size
      shade_terminals = true, -- Shade background for terminals
   }
}


